<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Primi Passi Laravel</title>
</head>

<body>
    {{--
    <h1>{{ messaggio }}</h1>

    <ul>
        @foreach (nomeitems as nuovonomeitem)
        <li>{{ nuovonomeitem }}</li>
        @endforeach
    </ul>

    @if (count(nuovonomeitem) > 2)
    <span>Perfetto ci sono più di due elementi nell'array.</span>
    @else
    <span>Attenzione: l'array contiene solo due o meno elementi. Assicurati che siano almeno 3</span>
    @endif --}}

</body>

</html>
